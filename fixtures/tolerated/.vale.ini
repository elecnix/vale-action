# Same rules as the dirty fixture, one level down. The action's fail-on is what
# separates a red run from this one.
StylesPath = ../dirty/styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = demo
demo.Filler = warning
