# This repository's own rules. The action carries none; it reads these.
StylesPath = styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = house

# The fixtures are input to the tests, not prose anyone reads. The dirty one
# is required to be dirty.
[fixtures/**/*.md]
BasedOnStyles =
